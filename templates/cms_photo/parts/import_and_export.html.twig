<div class="row">
    <div class="col-4">
        <a target="_blank" class="btn btn-success btn-sm" href="{{ path('cms_photo_new') }}">New</a>
    </div>
    <div class="col-3">
        <a class="btn btn-outline-primary btn-sm" href="{{ path('cms_photo_export') }}">Export</a>
        <a class="btn btn-outline-primary btn-sm" href="{{ path('cms_photo_import') }}">Import</a>
    </div>
    <div class="col-3">
        <a target="_blank" class="btn btn-outline-primary btn-sm" href="{{ path('product_index') }}">Products</a>
        <a target="_blank" class="btn btn-outline-primary btn-sm" href="{{ path('cms_copy_index') }}">CMS Copy</a>
    </div>
    <div class="col-2 desktop">
        {% if is_granted('ROLE_SUPER_ADMIN') %}
            <a class="btn btn-danger btn-sm" href="{{ path('cms_photos_delete_all_files') }}">Delete All Photos (but
                keep record)</a>
        {% endif %}
    </div>
</div>
<br>